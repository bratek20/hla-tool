package ${packageName}.api
#foreach($vo in $customTypes.simpleList)

fun create${vo.name}(value: ${vo.type}): ${vo.name} {
    TODO()
}

fun get${vo.name}Value(it: ${vo.name}): ${vo.type} {
    TODO()
}
#end
#foreach($vo in $customTypes.complexList)

fun create${vo.name}(#foreach($f in $vo.fields)$f.name: $f.type.name()#if($foreach.hasNext), #end#end): ${vo.name} {
    TODO()
}

#foreach($field in $vo.fields)
fun $vo.getterName(${field.name})(it: ${vo.name}): ${field.type.name()} {
    TODO()
}#if($foreach.hasNext)


#end#end#end