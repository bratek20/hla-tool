package ${packageName}.api

import com.github.bratek20.architecture.exceptions.ApiException

#foreach($exception in $exceptions)
class $exception(message: String = "") : ApiException(message)#if($foreach.hasNext)


#end#end