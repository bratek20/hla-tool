#foreach($vo in $valueObjects)
class ${vo.name()} {
    constructor(
        #foreach($field in $vo.fields)
        public readonly $field.name: $field.type.name(),
        #end
    ) {}
}#if($foreach.hasNext)


#end#end