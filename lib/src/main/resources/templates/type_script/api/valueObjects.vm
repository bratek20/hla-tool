#foreach($vo in $valueObjects)
class ${vo.name()} {
    constructor(
        #foreach($field in $vo.fields)
        public readonly $field.declaration(),
        #end
    ) {}
}#if($foreach.hasNext)


#end#end