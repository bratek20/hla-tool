#foreach($exception in $exceptions)
class $exception extends ApiException<$exception> {
    constructor() {
        super($exception)
    }

    getType(): string {
        return "$exception"
    }
}#if($foreach.hasNext)


#end#end