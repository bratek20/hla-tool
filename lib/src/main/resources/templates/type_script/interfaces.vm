#foreach($interface in $interfaces)
interface $interface.name {
#foreach($method in $interface.methods)
    ${method.name}(#foreach($arg in $method.args)$arg.name: $arg.type, #{end}c: HandlerContext): $method.returnType
#end
}#if($foreach.hasNext)

#end#end