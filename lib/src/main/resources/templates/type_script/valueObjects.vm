#foreach($vo in $valueObjects.simpleList)

class ${vo.name} {
    constructor(
        public readonly value: ${vo.type}
    ) {}
}
#end
#foreach($vo in $valueObjects.complexList)

class ${vo.name} {
    constructor(
        #foreach($field in $vo.fields)
        public readonly $field.name: $field.type.name(),
        #end
    ) {}
}#if($foreach.hasNext)

#end#end