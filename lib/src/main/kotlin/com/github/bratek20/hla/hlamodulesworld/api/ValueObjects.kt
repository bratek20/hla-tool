// DO NOT EDIT! Autogenerated by HLA tool

package com.github.bratek20.hla.hlamodulesworld.api

import com.github.bratek20.hla.definitions.api.*
import com.github.bratek20.hla.parsing.api.*

data class WorldModuleGroup(
    private val name: String,
    private val modules: List<ModuleDefinition>,
    private val innerGroups: List<WorldModuleGroup> = emptyList(),
) {
    fun getName(): GroupName {
        return GroupName(this.name)
    }

    fun getModules(): List<ModuleDefinition> {
        return this.modules
    }

    fun getInnerGroups(): List<WorldModuleGroup> {
        return this.innerGroups
    }

    companion object {
        fun create(
            name: GroupName,
            modules: List<ModuleDefinition>,
            innerGroups: List<WorldModuleGroup> = emptyList(),
        ): WorldModuleGroup {
            return WorldModuleGroup(
                name = name.value,
                modules = modules,
                innerGroups = innerGroups,
            )
        }
    }
}