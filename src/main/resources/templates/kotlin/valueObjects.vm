package ${packageName}.api
#if($imports.size() > 0)

#foreach($import in $imports)
import ${import}.api.*
#end

#end

#foreach($vo in $valueObjects.simpleList)
data class ${vo.name}(
    val value: ${vo.type}
)

#end
#foreach($vo in $valueObjects.complexList)
data class ${vo.name}(
    #foreach($field in $vo.fields)
    val $field.name: $field.type,
    #end
)
#if($foreach.hasNext)

#end
#end