package com.some.pkg.somemodule.api

import com.some.pkg.othermodule.api.*
import com.some.pkg.typesmodule.api.*

fun customAmountCreate(value: Int): CustomAmount {
    TODO()
}

fun customAmountGetValue(it: CustomAmount): Int {
    TODO()
}

fun customAmountRangeCreate(min: CustomAmount, max: CustomAmount): CustomAmountRange {
    TODO()
}

fun customAmountRangeGetMin(it: CustomAmountRange): CustomAmount {
    TODO()
}

fun customAmountRangeGetMax(it: CustomAmountRange): CustomAmount {
    TODO()
}
fun dateRangeWrapperCreate(range: DateRange): DateRangeWrapper {
    TODO()
}

fun dateRangeWrapperGetRange(it: DateRangeWrapper): DateRange {
    TODO()
}