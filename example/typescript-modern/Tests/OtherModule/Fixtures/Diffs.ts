// DO NOT EDIT! Autogenerated by HLA tool

import { OtherData } from "../../../main/OtherModule/Api/DataClasses"
import { OtherClass, OtherClassWIthUniqueId, OtherId, OtherProperty } from "../../../main/OtherModule/Api/ValueObjects"

export function diffOtherId(given: OtherId, expected: number, path: string = ""): string {
    if (given.getValue() != expected) { return `${path}value ${given.getValue()} != ${expected}` }
    return ""
}

export interface ExpectedOtherProperty {
    id?: number,
    name?: string,
}
export function diffOtherProperty(given: OtherProperty, expected: ExpectedOtherProperty, path: string = ""): string {
    const result: string[] = []

    if (expected.id !== undefined) {
        const expectedId = expected.id
        if (diffOtherId(given.getId(), expectedId) != "") { result.push(diffOtherId(given.getId(), expectedId, `${path}id.`)) }
    }

    if (expected.name !== undefined) {
        const expectedName = expected.name
        if (given.getName() != expectedName) { result.push(`${path}name ${given.getName()} != ${expectedName}`) }
    }

    return result.join("\n")
}

export interface ExpectedOtherClass {
    id?: number,
    amount?: number,
}
export function diffOtherClass(given: OtherClass, expected: ExpectedOtherClass, path: string = ""): string {
    const result: string[] = []

    if (expected.id !== undefined) {
        const expectedId = expected.id
        if (diffOtherId(given.getId(), expectedId) != "") { result.push(diffOtherId(given.getId(), expectedId, `${path}id.`)) }
    }

    if (expected.amount !== undefined) {
        const expectedAmount = expected.amount
        if (given.getAmount() != expectedAmount) { result.push(`${path}amount ${given.getAmount()} != ${expectedAmount}`) }
    }

    return result.join("\n")
}

export interface ExpectedOtherClassWIthUniqueId {
    uniqueId?: string,
}
export function diffOtherClassWIthUniqueId(given: OtherClassWIthUniqueId, expected: ExpectedOtherClassWIthUniqueId, path: string = ""): string {
    const result: string[] = []

    if (expected.uniqueId !== undefined) {
        const expectedUniqueId = expected.uniqueId
        if (given.getUniqueId() != expectedUniqueId) { result.push(`${path}uniqueId ${given.getUniqueId()} != ${expectedUniqueId}`) }
    }

    return result.join("\n")
}

export interface ExpectedOtherData {
    id?: number,
}
export function diffOtherData(given: OtherData, expected: ExpectedOtherData, path: string = ""): string {
    const result: string[] = []

    if (expected.id !== undefined) {
        const expectedId = expected.id
        if (diffOtherId(given.getId(), expectedId) != "") { result.push(diffOtherId(given.getId(), expectedId, `${path}id.`)) }
    }

    return result.join("\n")
}
