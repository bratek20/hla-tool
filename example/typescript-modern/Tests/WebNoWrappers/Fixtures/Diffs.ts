// DO NOT EDIT! Autogenerated by HLA tool

import { WebRequest, WebResult } from "../../../main/WebNoWrappers/Api/ValueObjects"

export interface ExpectedWebRequest {
    inputField?: number,
}
export function diffWebRequest(given: WebRequest, expected: ExpectedWebRequest, path: string = ""): string {
    const result: string[] = []

    if (expected.inputField !== undefined) {
        const expectedInputField = expected.inputField
        if (given.getInputField() != expectedInputField) { result.push(`${path}inputField ${given.getInputField()} != ${expectedInputField}`) }
    }

    return result.join("\n")
}

export interface ExpectedWebResult {
    outputField?: number,
}
export function diffWebResult(given: WebResult, expected: ExpectedWebResult, path: string = ""): string {
    const result: string[] = []

    if (expected.outputField !== undefined) {
        const expectedOutputField = expected.outputField
        if (given.getOutputField() != expectedOutputField) { result.push(`${path}outputField ${given.getOutputField()} != ${expectedOutputField}`) }
    }

    return result.join("\n")
}
