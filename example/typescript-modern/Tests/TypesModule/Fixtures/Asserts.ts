// DO NOT EDIT! Autogenerated by HLA tool

import { Date, DateRange, Tier } from "../../../main/TypesModule/Api/CustomTypes"
import { ExpectedDateRange, diffDate, diffDateRange, diffTier } from "./Diffs"

export function date(given: Date, expected: string) {
    const diff = diffDate(given, expected)
    AssertEquals(diff, "", diff)
}

export function tier(given: Tier, expected: number) {
    const diff = diffTier(given, expected)
    AssertEquals(diff, "", diff)
}

export function dateRange(given: DateRange, expected: ExpectedDateRange) {
    const diff = diffDateRange(given, expected)
    AssertEquals(diff, "", diff)
}
