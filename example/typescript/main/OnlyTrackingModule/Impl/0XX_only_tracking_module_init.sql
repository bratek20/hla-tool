-- Autogenerated by HLA tool

CREATE TABLE only_tracking_dimension (
    only_tracking_dimension_id BIGINT DEFAULT NEXTVAL('common.the_sequence'::regclass) CONSTRAINT only_tracking_dimension_id PRIMARY KEY,
    some_field INTEGER NOT NULL
);

CREATE TABLE only_tracking_event (
    CONSTRAINT only_tracking_event_id PRIMARY KEY (event_id),
    only_tracking_dimension_id BIGINT NOT NULL,
    optional_int INTEGER
) INHERITS (event);
ALTER TYPE event_type ADD VALUE 'only_tracking_event';