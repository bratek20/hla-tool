// DO NOT EDIT! Autogenerated by HLA tool

namespace OtherModule.Diff {
    export function otherId(given: OtherId, expected: number, path: string = "") {
        if (given.value != expected) { return `${path}value ${given.value} != ${expected}` }
        return ""
    }

    export interface ExpectedOtherProperty {
        id?: number,
        name?: string,
    }
    export function otherProperty(given: OtherProperty, expected: ExpectedOtherProperty) {
        const result: string[] = []

        if (expected.id !== undefined) {
            result.push(otherId(given.id, expected.id, "id."))
        }

        if (expected.name !== undefined) {
            if (given.name != expected.name) { result.push(`name ${given.name} != ${expected.name}`) }
        }

        return result.filter(x => x.length > 0).join("\n")
    }

    export interface ExpectedOtherClass {
        id?: number,
        amount?: number,
    }
    export function otherClass(given: OtherClass, expected: ExpectedOtherClass) {
        const result: string[] = []

        if (expected.id !== undefined) {
            result.push(otherId(given.id, expected.id, "id."))
        }

        if (expected.amount !== undefined) {
            if (given.amount != expected.amount) { result.push(`amount ${given.amount} != ${expected.amount}`) }
        }

        return result.filter(x => x.length > 0).join("\n")
    }
}